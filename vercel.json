{
  "version": 2,
  "builds": [
    { "src": "app.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/assets/(.*)", "dest": "/static/landing/assets/$1" },
    { "src": "/(.*)", "dest": "app.py" }
  ]
}